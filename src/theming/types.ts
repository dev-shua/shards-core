export type ShardsTheme = {
  id: string;
  label: string;
  colors: {
    primary:   string;
    success:   string;
    danger:    string;
    warning:   string;
    info:      string;
    muted:     string;
    bg:        string;
    bgTitle:   string;
    text:      string;
    textMuted: string;
  };
};